//sum of all digits of a number
#include <stdio.h>
int main()
{
    int a,sum=0,d;
    printf("enter the number:");
    scanf("%d",&a);
    while(a>0)
    {
        d=a%10;
        sum=sum+d;
        a=a/10;
        
    }
    printf("sum is=%d",sum);
    return 0;
}
